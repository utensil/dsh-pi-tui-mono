{
  "name": "dsh-pi-tui-mono",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo: run the pi coding agent's terminal experience inside DeepSeek Harness — the real InteractiveMode as the front door, pi extensions as dsh plugins, and a migration kit for existing pi installations.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@10.4.1"
}
